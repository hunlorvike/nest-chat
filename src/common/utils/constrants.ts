export enum Routes {
    AUTH = 'auth',
    USER = 'user'
}

export enum Services {
    AUTH = 'AUTH_SERVICE',
    USER = 'USER_SERVICE'
}

export enum Configs {
    JWTCONFIG = 'JWT_CONFIG'
}

export enum ApiTagConfigs {
    AUTH = 'Auth',
    USER = 'User'
}